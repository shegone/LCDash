# Qwen 3.6 27B Default - 2026-08-02

## Completed change

- Pulled `qwen3.6:27b` into the Ollama container on production host `14.1.1.227`.
- Confirmed the installed model is 27.8B parameters, Q4_K_M, with completion, vision, tools, and thinking capabilities.
- Made `qwen3.6:27b` the Open WebUI default for newly created chats.
- Confirmed a new chat selects `qwen3.6:27b` automatically.
- Ran a minimal host-side inference health check; the model returned `QWEN36_READY`.

## PC15 access model

PC15 uses the protected `.227` model gateway.  It does **not** carry a second 17 GB copy of the model.  This preserves PC15 disk and RTX 3090 capacity for iClone, Unreal, and rendering while still giving its Computer/Open WebUI workflows access to the new default through the shared service.

## Rollback and safety

- `qwen3.5:27b` remains installed and enabled as the rollback model.
- No LCDash, CAD, database, dispatch, or station-alert service was restarted or changed.
- No credentials or CAD payloads were recorded in this note.

## Next practical check

From the PC15 Computer interface, start a **new** chat and confirm the model selector shows `qwen3.6:27b`.  Existing chats retain their already-selected model unless manually changed.
