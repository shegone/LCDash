# Latest Status - PC .15

Updated: 2026-08-01 20:51 America/New_York
State: FBX export and isolated Unreal import passed; motion is static; stopped safely
Agent/model: Hosted Codex supervising local qwen3.5:27b and PC15 Windows control

## Completed

- Connected the PC15 Computer workspace and compact Windows-control proxy.
- Verified iClone 8.74.5527.1 and its installed RLPy FBX API bindings.
- Created the disposable iClone project with a neutral female avatar and
  `Female Idle_1.rlMotion`; the saved project is 77,258,714 bytes.
- Exported `MAE_iClone_FBX_Test_Unreal.fbx` after a successful FBX license
  check; the FBX is 10,220,384 bytes.
- Imported the FBX into the isolated Unreal path
  `/Game/MAE_Test/DisposableFBX_20260801` using a guarded Python task.
- Verified 63 imported Unreal assets, including the skeletal mesh, skeleton,
  physics asset, animation, materials, and textures.
- Opened the skeletal-mesh and animation editors for supervised inspection.
- Diagnosed the lack of motion with a read-only raw-key analysis.

## Verification and findings

- iClone project creation: `RESULT: PASS`.
- iClone FBX export: `RESULT: PASS`.
- Unreal isolated import: `RESULT: PASS`.
- Unreal generated 63 assets and did not modify existing content.
- Unreal reported nonfatal duplicate-texture renames and missing bind-pose
  entries for several IK/control bones.
- Unreal also reported imported materials that need morph-target usage support.
- Animation duration: 341 frames / 5.683333 seconds.
- Animation tracks: 108.
- Changing tracks: 0.
- Static tracks: 108.
- Conclusion: the FBX contains a timed static pose. Unreal playback is working;
  the iClone motion-bake/export step must be corrected.

## Files and isolated outputs

- iClone project:
  `C:\MAE-Agent\tests\MAE_iClone_FBX_Test.iProject`
- FBX:
  `C:\MAE-Agent\tests\fbx_export\MAE_iClone_FBX_Test_Unreal.fbx`
- Unreal destination:
  `/Game/MAE_Test/DisposableFBX_20260801`
- Shared results:
  `MAE_iClone_FBX_Export.result.txt`,
  `MAE_Unreal_FBX_Import.result.txt`,
  `MAE_Unreal_Animation_Check.result.txt`, and
  `MAE_Unreal_Open_Assets.result.txt`.
- Canonical helpers are under repository `scripts/` and copied into the shared
  handoff folder for PC15.

## Git state

- Repository: `E:\Projects\LCDash`
- Branch: `deployment/ubuntu-nvidia-227`
- Latest completed diagnostic commit before this handoff: `47f5b86`
- Branch was ahead of the remote by 10 commits before this stopping-point
  documentation commit.
- Nothing from this PC15 package was deployed into `.227` production services.

## Safety and scope confirmation

- No production `.227` service was restarted or changed.
- No existing Unreal baseline asset was overwritten or deleted.
- No CAD data, credentials, network settings, station-alert behavior, or
  GovCloud work was changed.
- The on-premises LCDash/MAE platform remains separate from the future AWS
  GovCloud `NGA911 MAE Upgrade` path.
- Keep the current disposable artifacts as diagnostic evidence.

## Exact next action

In iClone, apply an unmistakable walking or waving motion and verify visible
movement before export. Export to a new non-overwriting FBX filename. Run
`scripts/unreal_check_disposable_animation.py` against the new animation and
require at least one changing bone track before importing another isolated
Unreal test folder. Do not overwrite the current project, FBX, or Unreal assets.

